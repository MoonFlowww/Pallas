<Page
    x:Class="Pallas.BasePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pallas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Upper Bar -->
        <local:Upperbar x:Name="Upperbar" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
        <!-- Navigation Menu (Sidebar) -->
        <local:NavigationMenu x:Name="Sidebar" Margin="0,50,0,0" HorizontalAlignment="Left" VerticalAlignment="Stretch" />
        <!-- Content Frame for displaying content pages -->
        <Frame x:Name="ContentFrame" Margin="200,50,0,0" />
    </Grid>
</Page>
